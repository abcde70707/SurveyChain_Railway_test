# ── 本地 Docker Compose 環境變數範本 ──────────────────────────────────────────
# 複製此檔為 .env 並填入實際值，.env 已在 .gitignore 中不會被 commit

# JWT 簽名金鑰（正式環境請換成長且隨機的字串）
JWT_SECRET=local-dev-secret-change-me-in-production

# 智能合約部署位址（Sepolia / Mainnet）
VITE_CONTRACT_ADDRESS=0xYourContractAddressHere
